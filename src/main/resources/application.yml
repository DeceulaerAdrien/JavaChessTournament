spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ChessTournament
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp-mail.outlook.com
    port: 587
    username: "adrienletest@outlook.fr"
    password: "%%ze!LiFC95!mALm"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
server:
  port: 8080